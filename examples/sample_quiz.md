# Python Programming Quiz

1. What is the output of the following Python code?

   ```python
   x = [1, 2, 3]
   print(len(x))
   ```

   a. 1
   b. 2
   *c. 3
   d. Error

2. Which of the following is the correct way to define a function in Python?

   *a. ```python
       def my_function():
           pass
       ```
   b. ```python
      function my_function():
          pass
      ```
   c. ```python
      func my_function():
          pass
      ```
   d. ```python
      define my_function():
          pass
      ```

3. What does the `append()` method do to a list?

   a. Removes the last element from the list
   *b. Adds an element to the end of the list
   c. Sorts the list in ascending order
   d. Reverses the list

4. Given the following code, what will be printed?

   ```python
   def greet(name="World"):
       return f"Hello, {name}!"
   
   print(greet())
   ```

   a. Hello, name!
   *b. Hello, World!
   c. Hello, !
   d. Error: missing argument

5. Which statement about Python dictionaries is TRUE?

   a. Dictionary keys must be integers
   *b. Dictionary keys must be immutable (hashable)
   c. Dictionaries maintain insertion order only in Python 2
   d. Dictionaries cannot contain nested data structures
